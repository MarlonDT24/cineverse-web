# === Servidor ===
server.port=8081

# === Base de Datos (Docker) ===
spring.datasource.url=jdbc:mysql://localhost:3306/cineverse_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=cineverse123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# === JPA / Hibernate ===
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# === Seguridad JWT ===
jwt.secret=ClaveSuperSecretaCineVerse2025ParaFirmarTokensDeAccesoSeguro
jwt.expiration=86400000